<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit <%= album.title %> - forFansOf</title>
    <link rel="stylesheet" href="/css/albums/edit.css">
    <% include ../partials/resources.ejs %>
  </head>
  <body>
    <% include ../partials/header.ejs %>

    <form action="/albums/<%= album.albumURL %>" method="post">
      <input type="text" name="title" value="<%= album.title %>" />
      <input type="text" name="img" value="<%= album.img %>" />
      <input type="number" name="year" value="<%= album.year %>" />
      <input type="text" name="genres" value="<%= album.genres %>" />
      <input type="text" name="forFansOf" value="<%= album.forFansOf %>" />
      <input type="text" name="tracks" value="<%= album.tracks %>" />
      <button type="submit" name="button">Submit</button>
    </form>

    <% include ../partials/footer.ejs %>
  </body>
</html>
